[project]
name = "project-chimera"
version = "0.1.0"
description = "Golden environment setup with MCP Sense and uv for project chimera"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.5",
]
